{
    "installCommand": "npm install --legacy-peer-deps",
    "buildCommand": "npm run build",
    "rewrites": [
        {
            "source": "/api/v1/:path*",
            "destination": "/api/index.js"
        }
    ]
}