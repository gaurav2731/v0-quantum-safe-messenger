/** @type {import('next').NextConfig} */
const nextConfig = {
    outputFileTracingRoot: __dirname,
    // We specify the pages/app dir manually if needed, but usually this fix is enough
};

module.exports = nextConfig;
